import { useEffect, useState, type FormEvent, type ReactNode } from 'react'
import { Pencil, Plus, Power } from 'lucide-react'
import { Alert, AlertDescription } from './components/ui/alert'
import { Badge } from './components/ui/badge'
import { Button } from './components/ui/button'
import { Card } from './components/ui/card'
import { Input } from './components/ui/input'
import { Label } from './components/ui/label'
import { EmptyState, FormCard, PageToolbar } from './components/ui/page'
import { Select } from './components/ui/select'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from './components/ui/table'

type Row = Record<string, unknown> & { id: string }
const apiBase = import.meta.env.VITE_API_BASE_URL || '/api'
export function Accounts({ token }: { token: string }) {
  const [users,setUsers]=useState<Row[]>([]),[tutors,setTutors]=useState<Row[]>([]),[showForm,setShowForm]=useState(false),[editing,setEditing]=useState<Row|null>(null),[error,setError]=useState(''),[submitting,setSubmitting]=useState(false)
  const load=()=>Promise.all([request('/users',token),request('/tutor',token)]).then(([u,t])=>{setUsers(u);setTutors(t)}).catch(e=>setError(String(e)));useEffect(()=>{void load()},[token])
  async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setSubmitting(true);const form=Object.fromEntries(new FormData(e.currentTarget));const body:Record<string,unknown>={...form,isActive:form.isActive==='true'};if(body.role!=='guru')body.tutorId=null;if(!body.password)delete body.password;try{await request('/users'+(editing?'/'+editing.id:''),token,editing?'PUT':'POST',body);setShowForm(false);setEditing(null);void load()}catch(err){setError(String(err))}finally{setSubmitting(false)}}
  async function toggle(user:Row){try{await request('/users/'+user.id,token,'PUT',{username:user.username,email:user.email,role:user.role,tutorId:user.tutorId||null,isActive:!user.isActive});void load()}catch(e){setError(String(e))}}
  return <div className='space-y-4'><PageToolbar title="Manajemen Akun Pengguna" description={`${users.length} akun pengguna terdaftar dalam sistem.`} actions={<Button onClick={()=>{setEditing(null);setShowForm(true)}}><Plus className='h-4 w-4'/>Buat akun baru</Button>}/>{error&&<Alert className='border-destructive/40 text-destructive'><AlertDescription>{error}</AlertDescription></Alert>}{showForm&&<FormCard title={editing?'Ubah akun':'Buat akun'} description={editing?'Kosongkan kata sandi bila tidak ingin menggantinya.':'Hubungkan akun Tutor ke data profil tutor.'}><form className='grid gap-4 sm:grid-cols-2 lg:grid-cols-3' onSubmit={save}><Field label='Username'><Input name='username' defaultValue={String(editing?.username||'')} required/></Field><Field label='Email'><Input name='email' type='email' defaultValue={String(editing?.email||'')} required/></Field><Field label={editing?'Kata sandi baru (opsional)':'Kata sandi'}><Input name='password' type='password' minLength={8} required={!editing}/></Field><Field label='Peran'><Select name='role' defaultValue={String(editing?.role||'guru')}><option value='guru'>Tutor</option><option value='admin'>Admin</option><option value='kepala_sekolah'>Kepala Sekolah</option></Select></Field><Field label='Tutor'><Select name='tutorId' defaultValue={String(editing?.tutorId||'')}><option value=''>Pilih tutor</option>{tutors.map(t=><option key={t.id} value={t.id}>{String(t.nama)}</option>)}</Select></Field><Field label='Status'><Select name='isActive' defaultValue={String(editing?.isActive??true)}><option value='true'>Aktif</option><option value='false'>Nonaktif</option></Select></Field><div className='flex gap-2 sm:col-span-2 lg:col-span-3'><Button disabled={submitting}>{submitting?'Menyimpan...':'Simpan akun'}</Button><Button variant='outline' type='button' onClick={()=>{setShowForm(false);setEditing(null)}}>Batal</Button></div></form></FormCard>}<Card className="rounded-2xl border border-border bg-card shadow-2xs overflow-hidden"><Table><TableHeader><TableRow className="border-b border-border"><TableHead>Username</TableHead><TableHead>Peran</TableHead><TableHead>Tutor</TableHead><TableHead>Status</TableHead><TableHead className='text-right '>Aksi</TableHead></TableRow></TableHeader><TableBody>{users.map(u=><TableRow key={u.id}><TableCell><div className='font-semibold text-foreground'>{String(u.username)}</div><div className='text-xs text-muted-foreground'>{String(u.email||'-')}</div></TableCell><TableCell className='capitalize text-xs font-semibold'>{u.role === 'guru' ? 'Tutor' : String(u.role).replace('_',' ')}</TableCell><TableCell className="text-xs">{String(tutors.find(t=>t.id===u.tutorId)?.nama||'-')}</TableCell><TableCell><Badge variant={u.isActive?'secondary':'outline'}>{u.isActive?'Aktif':'Nonaktif'}</Badge></TableCell><TableCell><div className='flex justify-end gap-2'><Button size='sm' variant='outline' onClick={()=>{setEditing(u);setShowForm(true)}}><Pencil className='h-3.5 w-3.5'/>Ubah</Button><Button size='sm' variant='ghost' onClick={()=>void toggle(u)}><Power className='h-3.5 w-3.5'/>{u.isActive?'Nonaktifkan':'Aktifkan'}</Button></div></TableCell></TableRow>)}{!users.length&&<EmptyState colSpan={5}/>}</TableBody></Table></Card></div>
}
function Field({label,children}:{label:string;children:ReactNode}){return <div className='grid gap-2'><Label>{label}</Label>{children}</div>}
async function request(path:string,token:string,method='GET',body?:unknown){const r=await fetch(apiBase+path,{method,credentials:'include',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:body?JSON.stringify(body):undefined});const result=r.status===204?null:await r.json().catch(()=>({}));if(!r.ok)throw new Error(result?.error||'Permintaan gagal');return result}
